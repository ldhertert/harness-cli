query workflows($filters: [WorkflowFilter], $limit: Int = 100, $offset: Int) {
  result: workflows(filters: $filters, limit: $limit, offset: $offset) {
   nodes {
      id
      name
    }

    pageInfo {
      hasMore
    }
  }
}