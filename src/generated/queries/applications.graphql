query applications($filters: [ApplicationFilter], $limit: Int = 100, $offset: Int) {
  result: applications(filters: $filters, limit: $limit, offset: $offset) {
   nodes {
      id
      name
    }

    pageInfo {
      hasMore
    }
  }
}