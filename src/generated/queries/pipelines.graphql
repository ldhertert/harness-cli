query pipelines($filters: [PipelineFilter], $limit: Int = 100, $offset: Int) {
  result: pipelines(filters: $filters, limit: $limit, offset: $offset) {
   nodes {
      id
      name
    }

    pageInfo {
      hasMore
    }
  }
}