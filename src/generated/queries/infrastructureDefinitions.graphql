query infrastructureDefinitions($filters: [InfrastructureDefinitionFilter], $limit: Int = 100, $offset: Int) {
  result: infrastructureDefinitions(filters: $filters, limit: $limit, offset: $offset) {
   nodes {
      id
      name
    }

    pageInfo {
      hasMore
    }
  }
}