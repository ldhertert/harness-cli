query tags($filters: [TagEntityFilter], $limit: Int = 100, $offset: Int) {
  result: tags(filters: $filters, limit: $limit, offset: $offset) {
   nodes {
      id
      name
    }

    pageInfo {
      hasMore
    }
  }
}