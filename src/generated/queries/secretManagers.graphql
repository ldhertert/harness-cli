query secretManagers($filters: [SecretManagerFilter], $limit: Int = 100, $offset: Int) {
  result: secretManagers(filters: $filters, limit: $limit, offset: $offset) {
   nodes {
      id
      name
    }

    pageInfo {
      hasMore
    }
  }
}