query audits($filters: [ChangeSetFilter], $limit: Int = 100, $offset: Int) {
  result: audits(filters: $filters, limit: $limit, offset: $offset) {
   nodes {
      id
      name
    }

    pageInfo {
      hasMore
    }
  }
}