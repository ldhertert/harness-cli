query auditChangeContent($filters: [ChangeContentFilter], $limit: Int = 100, $offset: Int) {
  result: auditChangeContent(filters: $filters, limit: $limit, offset: $offset) {
   nodes {
      id
      name
    }

    pageInfo {
      hasMore
    }
  }
}