query connectors($filters: [ConnectorFilter], $limit: Int = 100, $offset: Int) {
  result: connectors(filters: $filters, limit: $limit, offset: $offset) {
   nodes {
      id
      name
    }

    pageInfo {
      hasMore
    }
  }
}